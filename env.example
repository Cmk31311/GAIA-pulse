# GAIA CODE API Configuration
# Copy this file to .env.local (for Next.js) or .env (for React)
# Keep your .env.local / .env file in .gitignore

# Live AWS API Gateway endpoint
NEXT_PUBLIC_API_BASE=https://your-api-gateway-id.execute-api.us-east-1.amazonaws.com

# Optional: API timeout in milliseconds
NEXT_PUBLIC_API_TIMEOUT=10000

